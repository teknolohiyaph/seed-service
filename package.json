{
  "name": "seed-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "start": "./node_modules/.bin/netlify-lambda serve src",
    "build": "npm install && npm audit fix --force && ./node_modules/.bin/netlify-lambda build src"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "netlify-lambda": "^2.0.16",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.0.2"
  }
}
